- 采用基于文件系统的分层目录结构：`Images/` 存储 SVG/PNG 图标与界面素材，`Languages/` 存放 INI 格式的多语言键值对，`Splash/` 与 `wallpaper/` 管理启动与背景视觉资源。
- 通过 `Config.ini` 定义全局运行时行为（如日志开关、字体设置、摄像头 ROI 参数）及多套医院报告模板的元数据，作为应用程序的默认配置中心。
- `anydata.db` 提供轻量级嵌入式数据存储能力，配合静态资源共同构成应用的离线运行基础。