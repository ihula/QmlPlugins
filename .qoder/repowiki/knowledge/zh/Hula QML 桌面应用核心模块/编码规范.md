- 所有核心管理类（如 DbManager, Configer）均继承自 QObject 并使用 SINGLETON 宏实现线程安全的单例模式。
- C++ 与 QML 之间的复杂数据交互统一使用 QJsonObject 和 QJsonArray 进行序列化传输。
- 全局错误码和业务状态统一定义在 common.h 的枚举类中，并通过 MessageInfo 结构体在模块间传递。
- 数据库操作采用 RAII 模式的 TransactionGuard 确保事务的原子性，未提交时自动回滚。